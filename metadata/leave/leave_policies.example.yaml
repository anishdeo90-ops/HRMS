items:
  - key: leave_policy.example.standard
    label: Standard Leave Policy Example
    domain: leave
    kind: leave_policy
    owner: hr
    source_ref: { system: frappe_hrms, module: leave, artifact: Leave Policy example }
    introduced_in_phase: 1
    db_table: metadata_registry
    ts_export: METADATA_KEYS
    api_routes: [app/api/leaves/policies]
    ui_surfaces: ["app/(dashboard)/leave/policies/page.tsx"]
    tests: [tests/metadata/registry-contract.test.ts]
